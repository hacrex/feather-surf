[package]
name = "feathersurf-beta-testing"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Beta testing and verification for FeatherSurf"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
thiserror = "2"

[dev-dependencies]
proptest = "1"
