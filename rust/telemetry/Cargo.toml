[package]
name = "feathersurf-telemetry"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Structured logging and privacy-safe diagnostics for FeatherSurf"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
proptest = "1"
